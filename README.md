JSON -> Contains the JSON Scheme used to validate JSON and XML API requests  
API.py -> Python code that runs the API on localhost  
AIS_TX.py -> The Python code that runs GNURadio and accepts a binary payload  
